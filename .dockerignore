# VCS and meta
.git
.gitignore

# Dependencies and build artifacts
vendor
node_modules

# Laravel transient files
storage/logs
storage/framework/cache
storage/framework/sessions
storage/framework/views

# Env/secrets (never bake into image)
.env

# If you prefer to exclude docker assets generally, whitelist the ones needed for COPY:
docker/*
!docker/composer
!docker/entrypoint.sh
!docker/app-healthcheck.sh

